import styled from '@emotion/styled';

const Container = styled.div({
  display: 'flex',
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center',
});

export const S = {
  Container,
} as const;
