import styled from '@emotion/styled';

const WorkingTimeTypeAndProjectSelectWrapper = styled.div({
  maxWidth: '19rem',
  maxHeight: '2.75rem',
  margin: 'auto',
});

export const S = { WorkingTimeTypeAndProjectSelectWrapper } as const;
